{% include '_header.twig' %}

{# get content by texonomy -> structure slug  #}


{% set contenttype = structurecontenttype(record.slug) %}

{% if contenttype %}
	{% setcontent records = '(Structures, ' ~ contenttype['name'] ~ ')' where { tree : record.slug } %}
	{% set records = sortRecords(records, 'position') %}
{% else %}
	{% setcontent records = 'Structures' where { tree : record.slug } %}
{% endif %}

	

{#
{% setcontent structureRecords = 'structure' where { tree  : record.slug} %}
{% setcontent pageRecords = 'page' where { tree : record.slug} %}
{% set records =  structureRecords|merge(pageRecords) %}

#}


{# order records by position #}
{#
{% for pageRecord in pageRecords %}

	{% for structureRecord in structureRecords %}
		
		{% if (pageRecord.group.order <= structureRecord.group.order ) %}
			{{ print(pageRecord.group.order) }}
		
		{% endif %}
	
	{% endfor %}
{% endfor %}
#}


{% set contacts = record.related('persons') %}

<!-- Start content -->
<main role="main" class="l-page">
<div class="lc">
    <!-- Start grid -->
    <div class="grid-main">


        <div class="l-content">
            
            <div class="l-breadcrumbs">
				{% include 'items/_item-breadcrumb.twig' %}
			</div>
            
            <div class="maincontent">
                <div class="page-intro">
                    <h1 class="title">{{ record.title }}</h1>
                    <p>{{ record.teaser|raw }}</p>
                </div>
				{% if records is defined %}
				<ul class="l-next_options"><!--
             -->{% for record in records|order('group') %}<!--
                -->{% include 'items/_item-lineblock.twig' with {child : record} %}<!--
             -->{% endfor %}
                </ul>
                {% endif %}

                {#
                <div class="section-sitemap">
	                <div class="l-section-sitemap">
	                	{% include 'items/_item-linklist.twig' with {record : "Join the Network"} %}<!--
	                 -->{% include 'items/_item-linklist.twig' with {record : "Provide data"} %}<!--
	                 -->{% include 'items/_item-linklist.twig' with {record : "Re-use data"} %}
	                </div>
                </div>
 				#}
            </div>
        </div><!--

     --><div class="l-sidebar">
            <div class="inner">
				{% include 'items/_item-help.twig' with { records: contacts, title: "Need help?",  use_secondary_mail: record.secondary_mail } %}
            </div>

        </div>
    </div>
    <!-- End grid -->
</div>
</main>
<!-- End Content -->

{% include '_footer.twig' %}
